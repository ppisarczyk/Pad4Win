<Window x:Class="Pad4Win.PropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sfw="clr-namespace:SoftFluent.Windows"
        xmlns:local="clr-namespace:Pad4Win"
        mc:Ignorable="d"
        Title="Properties" Height="300" Width="300" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <sfw:PropertyGrid x:Name="PG" />
        <Grid Grid.Row="1">
            <Button Name="Ok" IsDefault="True" Content="OK" Margin="0,5,90,5" HorizontalAlignment="Right" Width="75" Click="OK_Click" />
            <Button Name="Cancel" Content="Cancel" Margin="0,5,10,5" HorizontalAlignment="Right" Width="75" Click="Cancel_Click" />
        </Grid>
    </Grid>
</Window>
